# 知识库管理系统 - 运维文档

## 一、系统架构

### 1.1 项目结构

```
knowledge-base-v2.0.0/
├── backend/                    # NestJS 后端服务
│   ├── src/                    # 源码目录
│   ├── database.sqlite         # SQLite 数据库文件
│   └── package.json            # 依赖配置
├── frontend/                   # React 前端应用
│   ├── src/                    # 源码目录
│   └── package.json            # 依赖配置
├── docker-compose.elasticsearch.yml  # Elasticsearch 容器配置
├── start-search.sh             # 检索服务启动脚本
└── docs/                       # 文档目录
    └── 检索系统配置指南.md      # 检索系统配置说明
```

### 1.2 服务清单

| 服务 | 端口 | 技术栈 | 依赖 |
|------|------|--------|------|
| 前端应用 | 5173 | React + Vite | Node.js |
| 后端服务 | 3000 | NestJS | Node.js, SQLite |
| Elasticsearch | 9200 | Elasticsearch 8.11.0 | Docker |
| Kibana（可选） | 5601 | Kibana | Docker, Elasticsearch |

---

## 二、环境要求

### 2.1 基础环境

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0
- **Docker**: >= 20.0.0（用于 Elasticsearch）
- **Python**: >= 3.8（用于部分文档解析）

### 2.2 端口占用

确保以下端口未被占用：
- `3000`: 后端服务
- `5173`: 前端开发服务器
- `9200`: Elasticsearch HTTP
- `9300`: Elasticsearch 内部通信
- `5601`: Kibana（可选）

---

## 三、服务启动流程

### 3.1 启动顺序

```
1. Docker Desktop（启动 Elasticsearch）
   ↓
2. 后端服务（NestJS）
   ↓
3. 前端应用（React + Vite）
   ↓
4. 建立索引（通过管理界面）
```

### 3.2 步骤一：启动 Docker（Mac）

```bash
# 启动 Docker Desktop（如果未启动）
# 点击 Launchpad 中的 Docker 图标

# 启动 Elasticsearch 容器
cd /Users/ouluwangji/Desktop/knowledge-base-v2.0.0
docker-compose -f docker-compose.elasticsearch.yml up -d

# 验证 Elasticsearch 是否启动成功
curl http://localhost:9200/
# 预期输出：{"name":"...","cluster_name":"docker-cluster","version":{"number":"8.11.0",...}}
```

### 3.3 步骤二：启动后端服务

```bash
cd /Users/ouluwangji/Desktop/knowledge-base-v2.0.0/backend

# 安装依赖（首次运行或依赖更新时）
npm install

# 编译 TypeScript
npm run build

# 启动服务（生产模式）
npm run start:prod

# 或开发模式（热重载）
npm run start:dev
```

**验证后端服务**：

```bash
curl http://localhost:3000/
# 预期输出：{"status":"ok","message":"Knowledge Base API"}
```

### 3.4 步骤三：启动前端应用

```bash
cd /Users/ouluwangji/Desktop/knowledge-base-v2.0.0/frontend

# 安装依赖（首次运行或依赖更新时）
npm install

# 启动开发服务器
npm run dev
```

**访问地址**：http://localhost:5173

### 3.5 步骤四：建立检索索引

1. 打开浏览器访问：http://localhost:5173
2. 使用管理员账号登录：
   - 用户名：`admin`
   - 密码：`admin123`
3. 点击顶部导航「检索配置」
4. 在「索引管理」区域，点击「重建全部索引」
5. 等待索引建立完成（提示 "Indexed X documents, 0 failed"）

---

## 四、服务管理命令

### 4.1 停止服务

```bash
# 停止前端（Ctrl + C）

# 停止后端（Ctrl + C）

# 停止 Elasticsearch
cd /Users/ouluwangji/Desktop/knowledge-base-v2.0.0
docker-compose -f docker-compose.elasticsearch.yml down

# 停止并删除数据（谨慎使用）
docker-compose -f docker-compose.elasticsearch.yml down -v
```

### 4.2 查看日志

```bash
# 查看后端日志
tail -f /tmp/backend.log

# 查看 Elasticsearch 日志
docker logs knowledge-base-elasticsearch -f

# 查看 Kibana 日志
docker logs knowledge-base-kibana -f
```

### 4.3 重启服务

```bash
# 重启后端服务
cd /Users/ouluwangji/Desktop/knowledge-base-v2.0.0/backend
kill $(lsof -t -i:3000) 2>/dev/null; sleep 2
npm run build && npm run start:prod

# 重启 Elasticsearch
docker restart knowledge-base-elasticsearch
```

---

## 五、数据库管理

### 5.1 SQLite 数据库

数据库文件位于：`backend/database.sqlite`

```bash
# 进入数据库
sqlite3 backend/database.sqlite

# 查看表结构
.tables

# 查询用户
SELECT id, username, email FROM user;

# 查询文档
SELECT id, title, viewCount FROM document;

# 退出
.exit
```

### 5.2 重置管理员密码

```bash
# 使用 bcrypt 加密新密码
node -e "const bcrypt = require('bcrypt'); bcrypt.hash('newpassword', 10).then(console.log)"

# 更新数据库
sqlite3 backend/database.sqlite "UPDATE user SET password='<hash>' WHERE username='admin';"
```

---

## 六、配置说明

### 6.1 环境变量

后端服务支持以下环境变量：

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `PORT` | 后端服务端口 | 3000 |
| `JWT_SECRET` | JWT 密钥 | knowledge_base_secret_key |
| `DB_PATH` | SQLite 数据库路径 | ./database.sqlite |

### 6.2 检索服务配置

登录系统后，在「检索配置」页面配置：

- **OpenAI API Key**: 用于 Whisper 语音转文字
- **阿里云 ASR Key**: 国内语音识别服务
- **腾讯云 ASR Key**: 国内语音识别服务
- **豆包 API Key**: 用于 OCR 文字识别

---

## 七、常见问题

### 7.1 Elasticsearch 启动失败

**问题**：`max virtual memory areas vm.max_map_count [65530] is too low`

**解决方案**：

```bash
# Linux/Mac
sudo sysctl -w vm.max_map_count=262144

# 永久生效（Linux）
echo "vm.max_map_count=262144" >> /etc/sysctl.conf
```

### 7.2 后端服务启动失败

**问题**：端口被占用

**解决方案**：

```bash
lsof -i:3000
kill -9 <PID>
```

### 7.3 前端无法访问后端

**问题**：跨域问题或后端未启动

**解决方案**：

1. 确保后端服务正在运行
2. 检查 Vite 代理配置（`frontend/vite.config.js`）

### 7.4 检索功能不生效

**问题**：搜索结果为空

**解决方案**：

1. 确保 Elasticsearch 正在运行
2. 进入「检索配置」页面建立索引
3. 检查后端日志是否有错误

---

## 八、安全注意事项

1. **生产环境**：修改 `JWT_SECRET` 为复杂密钥
2. **数据库备份**：定期备份 `database.sqlite` 文件
3. **Elasticsearch**：生产环境启用安全认证
4. **API Key**：不要将 API Key 提交到代码仓库
5. **文件上传**：限制上传文件大小和类型

---

## 九、版本信息

| 组件 | 版本 |
|------|------|
| NestJS | 11.1.24 |
| React | 18.x |
| Vite | 6.x |
| Elasticsearch | 8.11.0 |
| SQLite | 3.x |